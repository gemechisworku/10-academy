{
  "name": "canonicalize",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --experimental-specifier-resolution=node test/canonicalize.test.js && node --experimental-specifier-resolution=node test/retry.test.js"
  }
}
